import { XWheelDirective } from './x-wheel.directive';

describe('XWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new XWheelDirective();
    expect(directive).toBeTruthy();
  });
});
